package Costco.qa;

import java.util.ArrayList;

import org.openqa.selenium.By;
import org.testng.annotations.DataProvider;
import org.testng.annotations.Test;

import base.TestBase;
import configFiles.TestUtil;

public class register extends TestBase{
	
	/*
	 * @DataProvider public void getTestData() { ArrayList<Object[]> testData =
	 * TestUtil.getDataFromExcel(); return testData.iterator(); }
	 * 
	 * @Test(dataProvider="getTestData") public void costcoRegistration(String
	 * email, String password, String confirmPassword, String membership) {
	 * 
	 * driver.findElement(By.xpath("")).clear();
	 * driver.findElement(By.xpath("")).sendKeys(email);
	 * 
	 * driver.findElement(By.xpath("")).clear();
	 * driver.findElement(By.xpath("")).sendKeys(password);
	 * 
	 * driver.findElement(By.xpath("")).clear();
	 * driver.findElement(By.xpath("")).sendKeys(confirmPassword);
	 * 
	 * driver.findElement(By.xpath("")).clear();
	 * driver.findElement(By.xpath("")).sendKeys(membership); }
	 */
}
